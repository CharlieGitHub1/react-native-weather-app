import React from 'react';
import { Text } from 'react-native-paper';

type Props = {};

const WeatherForecast = (props: Props) => {
  return <Text>WeatherForecast</Text>;
};

export default WeatherForecast;
